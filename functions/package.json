{
  "name": "mylibrary-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "cheerio": "^1.0.0",
    "firebase-admin": "^13.6.1",
    "firebase-functions": "^6.3.2"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
